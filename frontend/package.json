{
  "name": "devops-bot-frontend",
  "version": "1.0.0",
  "description": "Frontend for DevOps bot project",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "echo 'Building the frontend...' && mkdir -p dist && cp -r public/* dist/"
  },
  "dependencies": {
    "node-fetch": "^2.6.1", // Add this line if not present
    "express": "^4.17.1"
  }
}
